spring:
  datasource:
    url: jdbc:postgresql://db:5432/edgetwin
    username: edgetwin
    password: edgetwin_dev

  sql:
    init:
      mode: never

  jpa:
    hibernate:
      ddl-auto: none

edge-ai:
  enabled: true
  base-url: http://edge-ai:8000
